import React, {useEffect, useState} from 'react';

const Orders = () => {
    const [state, setState] = useState([]);

    useEffect(async () => {

        setState(data)
    }, [])

    return (
        <div>

        </div>
    );
};

export default Orders;